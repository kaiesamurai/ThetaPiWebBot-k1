export enum UserRole {
  Admin = "admin",
  User = "user",
}

export enum LoginType {
  Google = "GOOGLE",
  Normal = "NORMAL",
}

export enum AuthKey {
  AccessToken = "AccessToken",
  RefreshToken = "RefreshToken",
}
